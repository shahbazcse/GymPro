const initialState = {
  exercise: [],
  goals: [],
  food: [],
};

const AppReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AppReducer;
